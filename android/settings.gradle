/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/7.6/userguide/multi_project_builds.html
 * This project uses @Incubating APIs which are subject to change.
 */

rootProject.name = 'vulkanLearning'

file('apps').eachDir { sub ->
    if (file("apps/$sub.name/build.gradle").exists()) {
        println("Adding project $sub.name")
        include ":$sub.name"
        project(":$sub.name").projectDir = new File("apps/$sub.name")
    }
}